# FIFO Common Component

Reusable FIFO component project for the harness workspace.

This repository is mounted as a submodule of the workspace root. It should
contain the FIFO design, verification collateral, and project-local rules needed
to develop the shared FIFO component.

